kind = "web"
previewPath = "/"
title = "Sistema de Panificación"
version = "1.0.0"
id = "artifacts/panaderia"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25522

[services.development]
run = "pnpm --filter @workspace/panaderia run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/panaderia", "run", "build" ]
publicDir = "artifacts/panaderia/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25522"
BASE_PATH = "/"
